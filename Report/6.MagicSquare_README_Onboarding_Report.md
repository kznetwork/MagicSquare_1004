# MagicSquare — README·온보딩·To-Do 정리 보고서

| 항목 | 내용 |
|------|------|
| 문서 목적 | 저장소 루트 `README.md` 신규 작성·보강, 구현 백로그(To-Do) 정렬 가이드, 개발·TDD 실행 환경 문서화 작업을 한곳에 기록한다. |
| 대상 범위 | `README.md`, 문서 참조 관계(PRD·Report·Cursor·`pyproject.toml`), 가상 환경·pytest·TDD·리팩터링 워크플로 |
| 선행 보고서 | Report/1~5, `docs/5.PRD_MagicSquare_4x4_TDD.md` |
| 작성일 | 2026-04-20 |

---

## 1. 작업 개요

다음 작업이 수행되었다.

1. **To-Do·문서 참조 가이드**  
   - 구현용 할 일 목록을 만들 때 **1순위 원천**으로 `docs/5.PRD_MagicSquare_4x4_TDD.md`를 두고, 스토리 분해는 **Report/4**, 계약·오류 세부는 **Report/2**, 실행·ECB·TDD는 **Report/3**·`pyproject.toml`·`.cursor/rules`를 참조할 것을 정리했다.

2. **구현용 To-Do 구조(개념 산출)**  
   - Concept-to-Code Traceability, Dual-Track(Boundary / Domain), ECB(Entity·Control·Boundary), RED–GREEN–REFACTOR를 적용한 **Epic / User Story / Phase별 Task** 초안이 대화를 통해 작성되었다.  
   - 식별자 규칙(Epic-001, US-001, TASK-001), Scenario Level(L0~L3), PRD의 TC-ID(U-/D-/E2E)·FR·컴포넌트명(`EmptyCellLocator` 등)과의 연결을 전제로 했다.  
   - **저장소 파일로는 전문을 보관하지 않았고**, 동등한 Phase 단위 요약은 아래 **산출물**의 `README.md` “구현 To-Do” 절에 반영되었다.

3. **`README.md` 신규 작성**  
   - 프로젝트 소개, **문서 가이드 표**, PRD 기준 요약·계약 오류 표·검증 기준 체크리스트, Phase 0~5 **구현 To-Do**, 현재 `src` 상태(User 엔티티 샘플) 안내를 포함했다.

4. **`README.md` 개발 환경 절 보강**  
   - **가상 환경(`.venv`)을 기본**으로 생성·활성화(Windows Git Bash / CMD / PowerShell, Linux·macOS·WSL) 절차를 상세화했다.  
   - `pip` 업그레이드, `pip install -e ".[dev]"`, `pytest` 및 `-k` 등 선택 실행 예시를 추가했다.  
   - **TDD·리팩터링:** `.cursor/rules/magicsquare-tdd-testing.mdc`와 맞춘 RED / GREEN / REFACTOR 표, PRD §8 Dual-Track 요약, 리팩터 시 커버리지 하한(규칙상 80%) 언급을 추가했다.  
   - **커버리지:** PRD NFR(Domain ≥95%, Boundary ≥85%)과 `pytest-cov` 선택 설치·실행 예시를 추가했다(`dev` optional에 `pytest-cov` 미포함은 명시).

---

## 2. 산출물

| 경로 | 설명 |
|------|------|
| `README.md` | 루트 온보딩 문서: 문서 맵, 요약·오류 표, **가상 환경·설치·테스트·TDD·리팩터링·커버리지**, 검증 기준, 구현 To-Do(Phase 0~5), 현재 코드베이스 상태 |

**참고:** 대화 전용으로만 존재하던 **상세 TASK 보드**(TASK-001~073 수준)는 본 보고서 본문에 전부 재실하지 않는다. 필요 시 이슈 트래커·프로젝트 보드로 옮겨 TC-ID와 링크를 유지할 것을 권장한다.

---

## 3. 근거 문서와의 정렬

| 근거 | 본 작업에서의 역할 |
|------|---------------------|
| `docs/5.PRD_MagicSquare_4x4_TDD.md` | README 본문·검증 기준·오류 문구·To-Do Phase의 **단일 진실 원천** |
| Report/4 | 스토리·Epic 관점의 To-Do 분해 시 참고(README “문서 가이드”) |
| Report/2 | `code`/`message` 요약 표, BR-10 순서 |
| Report/3, `pyproject.toml`, `.cursorrules`, `.cursor/rules/*.mdc` | venv·pytest·ECB·TDD 표 |
| Report/5 | README 상단 “문서 관계(한 줄)” 문구와 정합 |

---

## 4. README 구조 요약(목차 수준)

| 섹션 | 제목 |
|------|------|
| (머리글) | 프로젝트 한 줄 소개, Report/5 연계 문장 |
| 1 | 문서 가이드(표) |
| 2 | 요구사항 요약(PRD) |
| 3 | 계약 실패 `code` / `message` |
| 4 | 개발 환경 및 실행 — 사전 요구, **venv**, 설치, pytest, **pytest-cov(선택)**, **TDD·REFACTOR 표**, 소스·ECB |
| 5 | 검증 기준(PRD FR·§9.3·Dual-Track·NFR) |
| 6 | 구현 To-Do(Phase 0~5) |
| 7 | 현재 저장소 상태(User 엔티티) |
| 8 | 라이선스(미정 안내) |

---

## 5. 의도적 비범위·후속 권장

| 항목 | 내용 |
|------|------|
| `pyproject.toml` 변경 없음 | `pytest-cov`는 README에서 **선택** 설치로만 안내. CI 게이트를 고정하려면 `[project.optional-dependencies].dev`에 추가하는 패치를 권장한다. |
| Magic Square 구현 코드 | 본 라운드에서는 **문서만** 추가·수정했다. 구현은 README “구현 To-Do” 및 PRD에 따른다. |
| 상세 TASK-xxx 전문 | 저장소에 별도 `docs/Implementation_Backlog.md` 등으로 보관할지 팀에서 결정한다. |

---

## 6. 검증(본 보고서 자체)

- [x] 루트 `README.md`가 존재하고, PRD·Report·Cursor 규칙을 가리키는 링크가 상대 경로로 유효한지(수동 확인 권장).
- [x] 가상 환경 절이 Windows·Unix 계열을 혼동 없이 구분했는지(`.venv/Scripts` vs `.venv/bin`).

---

## 문서 끝

본 보고서는 **README 온보딩·TDD 실행 환경 문서화** 및 그에 앞선 **To-Do 참조 가이드·구조 설계** 작업의 기록이다. 제품 요구사항 변경은 `docs/5.PRD_MagicSquare_4x4_TDD.md`의 버전·변경 이력으로 관리한다.
